language: node_js
node_js:
  - '10'

cache:
  yarn: true
  directories:
    - node_modules

install:
  - yarn install --frozen-lockfile

jobs:
  include:
    - stage: Build and test
      script:
        - yarn build
        - yarn test:ci
    - stage: npm release
      script:
        - yarn build
      deploy:
        provider: npm
        email: '$NPM_EMAIL'
        api_key: '$NPM_TOKEN'
        skip_cleanup: true
        on:
          branch: master
          tags: true
      before_deploy:
        - git config --global user.email "alisonmsn@hotmail.com"
        - git config --global user.name "slipalison"
        - git config --global push.default matching
        - git config credential.helper "store --file=.git/credentials"
        - echo "https://${GH_TOKEN}:@github.com" > .git/credentials
        - git checkout -- .
        - yarn version --patch
        - git status
        - git push origin HEAD:$TRAVIS_BRANCH
        - git push --tags
        # provider: script
        # skip_cleanup: true
        # on:
        #   tags: false
        # - git config --global push.default matching
        # - git config --global user.email "alisonmsn@hotmail.com"
        # - git config credential.helper "store --file=.git/credentials"
        # - echo "https://${GitHub_Token}:@github.com" > .git/credentials
        # - git add --all
        # - git commit -m 'some message'
        # - git push https://${GH_TOKEN}@github.com/slipalison/apollo-odata-resolver.git
        # - git push https://${GH_TOKEN}@github.com/slipalison/apollo-odata-resolver.git --tags

branches:
  only:
    - master
    - /^v[0-9]+.*$/
